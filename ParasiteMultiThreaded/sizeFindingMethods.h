#ifndef sizeFindingMethods
#define sizeFindingMethods

int checkPixels(unsigned char *anArray, int i, int j, int findColour, int replaceColour);

int* findPixels(int height, int width, unsigned char *anArray);

#endif